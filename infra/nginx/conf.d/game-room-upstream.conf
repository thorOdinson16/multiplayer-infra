# Default match -> game-room leader routing map.
# This file is owned by the etcd-watcher service, which overwrites it
# atomically whenever /match/{id}/leader-address keys change. It is checked
# in so that NGINX can start before the watcher has produced anything.
map $arg_match_id $game_room_upstream {
    default game_room_default;
}
